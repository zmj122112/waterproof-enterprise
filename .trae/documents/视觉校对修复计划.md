# 视觉校对修复计划

## 任务1：统一首页头部样式与 Logo (Home.vue)
1. **Logo 样式**：
   - 修改 Logo 为 `<img src="/logo.png" />`
   - 添加类名：`w-10 h-10 rounded-full bg-white p-0.5 border-2 border-red-200`
   - 右侧文字加粗加大字号

2. **解决重叠**：
   - 红色 Header 底部内边距增加到 `pb-24`
   - 调整搜索框容器布局，确保不盖住 Header 文字
   - 调整核心服务区域上边距，确保与红色 Header 有清晰界限

## 任务2：修复个人中心布局灾难 (UserCenter.vue)
1. **排版混乱修复**：
   - 标题栏使用 `flex justify-between items-center px-4 py-3`
   - 左侧放“我的订单”标题，右侧放“全部订单 >”链接（灰色小字）
   - 图标栏使用 Grid 布局：`grid grid-cols-4 gap-2 py-4`
   - 每个子项使用 `flex flex-col items-center` 确保垂直居中对齐

2. **消除红线 Bug**：
   - 给红色 Header 添加 `border-none`
   - 给悬浮白色订单卡片添加 `relative z-10` 和 `-mt-12`

## 任务3：协调首页按钮配色 (Home.vue)
- **快速报修（左）**：改为淡红风格
  - 背景：`bg-red-50`
  - 文字：`text-red-600`
  - 边框：`border border-red-100`
- **预约上门（右）**：保持深红风格
  - 背景：`bg-red-600`
  - 文字：`text-white`

## 任务4：内页联系卡片与全局按钮红化 (Appointment.vue / RepairEstimate.vue)
1. **布局修复**：
   - “如何联系我们”卡片使用 `flex flex-row items-center justify-between`
   - 左侧文字信息容器宽度：`w-2/3`
   - 右侧二维码容器宽度：`w-1/3`，右对齐

2. **图片替换**：
   - 二维码图片链接改为：`https://api.qrserver.com/v1/create-qr-code/?size=150x150&data=MoonStarCS`
   - 设置图片尺寸：`w-24 h-24`

3. **按钮红化**：
   - 页面底部“提交预约”按钮背景色从 `bg-blue-600` 改为 `bg-red-600`

## 修复文件列表
- `src/views/c-mini/Home.vue`
- `src/views/c-mini/UserCenter.vue`
- `src/views/c-mini/Appointment.vue`
- `src/views/c-mini/RepairEstimate.vue`