### 修复计划

**目标**：按照用户提供的严格规范，修复微信开发者工具中Home.vue和Layout.vue的样式问题

**修复内容**：

1. **Home.vue Template重写**：
   - 重写Header部分，使用指定的红色背景容器类
   - 修复Logo样式，使用指定的class和尺寸
   - 重写双按钮区，使用Grid布局和负边距嵌入设计
   - 确保所有文字和按钮样式符合要求

2. **Layout.vue重写**：
   - 重写底部TabBar容器，使用fixed定位和Grid布局
   - 修复导航项样式，确保选中和未选中状态正确
   - 调整图标和文字大小，确保布局对齐
   - 确保TabBar固定在底部，不影响页面内容

**技术要点**：
- 严格按照用户提供的class名称和样式规范
- 使用Tailwind CSS实现响应式布局
- 确保样式在微信浏览器中兼容
- 保持代码的可读性和可维护性

**预期效果**：
- Header恢复红色沉浸式背景，Logo和文字显示正常
- 核心按钮区恢复并排卡片样式，嵌入Header区域
- 底部TabBar样式规范，图标和文字对齐，选中状态高亮

**修改文件**：
- `src/views/c-mini/Home.vue` (Template部分)
- `src/views/c-mini/Layout.vue` (Template + Style部分)