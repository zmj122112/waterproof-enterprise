### 实施计划

**任务1：重构Home.vue首页头部**
1. 修改信任区数据：将现有的trustItems数组替换为用户要求的3个静态入口数据
2. 调整信任区布局：修改Grid布局的信任卡片，使其符合"信任金刚区"的设计要求
3. 更新信任区样式：
   - 增大图标尺寸
   - 文字居中对齐
   - 强化卡片阴影效果
   - 确保白色圆角卡片设计
4. 更新点击事件：将卡片点击事件修改为跳转到指定路由
5. 保留核心业务入口：确保"漏水维修"和"新房防水"两个大按钮不受影响

**任务2：创建通用详情页TrustDetail.vue**
1. 创建新文件：在src/views/c-mini/目录下创建TrustDetail.vue
2. 实现页面结构：
   - 顶部导航栏：包含返回按钮和动态标题
   - 内容区域：根据路由参数显示不同的静态内容
   - 悬浮底栏：固定在底部的"立即预约上门"红色按钮
3. 实现路由参数处理：根据type参数显示不同内容
4. 实现交互逻辑：返回按钮和预约按钮的点击事件

**任务3：配置路由**
1. 在router/index.js中为c-mini添加新的信任详情页路由
2. 配置动态路由参数：/trust/:type
3. 指向新创建的TrustDetail.vue组件

### 预期效果
- Home.vue首页头部显示3个静态信任卡片，样式符合要求
- 点击信任卡片跳转到对应的详情页
- TrustDetail.vue根据路由参数显示不同内容
- 详情页底部有固定的预约按钮
- 路由配置正确，页面可以正常访问